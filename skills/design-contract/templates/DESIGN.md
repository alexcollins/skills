---
# Replaced by scripts/design-tokens.mjs on its first run. Do not write tokens here by hand.
version: "alpha"
name: "<Project>"
---

<!-- Everything below the frontmatter is written by hand, from the code.
     The generator only ever rewrites the frontmatter above.
     Spec sections (Overview … Do's and Don'ts) keep this order and these exact headings.
     Delete every HTML comment before finishing. -->

# <Project> — design system

## Overview

<!-- One sentence naming a specific thing in the world this design is.
     "A darkroom light table: everything dim except the work." Not adjectives. -->

<!-- Then 3–6 short paragraphs, each a rule with its reason: -->

**<Rule name>.** <What the design does.> <Why — what it protects or makes possible.>

**Key characteristics:**

- <a property an agent can check a screen against>
- …

## Source of truth

| Value | Owned by | Change it there, then |
|---|---|---|
| Colours, radii, type scale | `<stylesheet>` | `npm run tokens` |
| <…> | `<file>` | `npm run tokens` |

The frontmatter above and [`design/tokens.json`](design/tokens.json) are generated by
`scripts/design-tokens.mjs`. `npm run tokens:check` fails the build when they drift from the code,
or when a new arbitrary value appears in markup.

## Colors

<!-- One sentence on the palette's character. Then each mapped colour. -->

- **<Descriptive name>** `{colors.background}` — <where it is used, and why>.
- **<Descriptive name>** `{colors.primary}` — <…>.

**The <Name> Rule.** <A short doctrine about colour, with its reason.>

## Typography

<!-- Families and why each exists; the role each plays; fluid or fixed and between which widths;
     how sizes are applied in code, and what is forbidden. -->

- **<Family>** — <role>. <Why this face.>
- Sizes go through <the mechanism>. Never <the bypass>, because <reason>.

## Layout

<!-- Shell, max width, gutter, section rhythm with the real classes, breakpoints and what changes.
     Full reference: design/layout.md -->

See [`design/layout.md`](design/layout.md) for the full reference.

## Elevation & Depth

<!-- Shadows, tonal layers, hairlines — or explicitly none, and how depth is conveyed instead. -->

## Shapes

<!-- Radius strategy and its reason; borders; nesting (outer = inner + padding). -->

## Motion

<!-- The curves by name and how many; durations by interaction; what never animates;
     reduced motion. Full reference: design/motion.md -->

See [`design/motion.md`](design/motion.md) for the full reference.

## Brand assets

| Asset | Path | Use |
|---|---|---|
| <Wordmark> | `<public/brand/…>` | <where> |

**Never recreate, redraw, approximate or re-type the logo** — not as SVG, not as a path, not as
text. Reference the file by path.

## Components

<!-- The primitives an agent builds with. Each: path, variants, and a Don't.
     Full inventory: design/components.md -->

- **<Button>** `<components/ui/button.tsx>` — <variants>. **Don't** <misuse>; use <alternative>.

See [`design/components.md`](design/components.md) for every component.

## Do's and Don'ts

### Do

- **Do** <specific prescription grounded in the code>.

### Don't

- **Don't** <specific prohibition>, because <reason>.
- **Don't** hand-edit `design/tokens.json` or the frontmatter above.

## Deliberate choices

<!-- Patterns this design uses on purpose that design tools flag as generic "tells":
     caps eyebrows, mono metadata, middle dots, → on links, near-black grounds, hairline rules,
     one radius everywhere, single-word emphasis. Say why each is intentional, so nobody "fixes" it.
     Delete the section if there are none. -->

- **<Choice>.** <Why it is the brand, not a default.>

## Known inconsistencies

Real deviations, deliberately left alone. Documented so nobody copies one as a pattern or "fixes"
one as a side effect of another change.

| What | Where | Why it stands |
|---|---|---|
| <finding from the audit> | `<file>` | <reason> |

## Before shipping UI

1. <The project's polish or review step, if any.>
2. `npm run tokens:check` · <typecheck> · <lint> · <build>
3. Capture the changed pages at 1440 and 390 and compare with [`design/references/`](design/references/).
